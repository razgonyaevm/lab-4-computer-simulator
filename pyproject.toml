[tool.ruff]
line-length = 120

[tool.ruff.lint]
# Включаем строгие правила проверки
select = ["E", "F", "W", "C90", "I", "N", "UP", "B", "A", "C4"]

[tool.mypy]
# Строгая проверка типов в mypy
disallow_untyped_defs = true
ignore_missing_imports = true
warn_unused_ignores = true